# Prompt Templates

Prompt-template loading is no longer implemented or exported from `packages/agent/src`.

`@tsuuanmi/pi-agent` currently exposes the core agent runtime, execution-environment contracts, message conversion helpers, extension contracts, subagent contracts, and utility modules listed in the [docs index](../index.md). Prompt-template discovery and invocation are higher-layer host responsibilities.
