# backends/anthropic/models

Mirrors `src/backends/anthropic/models.ts`.

The backend selects the package-aligned Haiku, Sonnet, and Opus models from `@tsuuanmi/pi-ai` so
reasoning maps, context limits, output limits, compatibility flags, and costs stay synchronized with
Pi's Anthropic transport.
