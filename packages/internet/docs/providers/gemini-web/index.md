# providers/gemini-web/index

Mirrors `src/providers/gemini-web/index.ts`.

Provider-local exports for Gemini Web model metadata, request adaptation, naming, config creation, and registration. The package composition root uses the generic provider registry.
