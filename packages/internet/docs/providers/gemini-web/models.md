# providers/gemini-web/models

Mirrors `src/providers/gemini-web/models.ts`.

Builds zero-cost, text-only Pi model metadata from the verified daemon capability marker. Only
account-visible Flash, Thinking, and Pro modes are registered. The browser provider uses conservative
32,000-token context and 8,192-token output limits and does not advertise reasoning controls.
