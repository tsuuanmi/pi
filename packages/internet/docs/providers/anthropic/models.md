# providers/anthropic/models

Mirrors `src/providers/anthropic/models.ts`.

The provider selects the package-aligned Haiku, Sonnet, and Opus models from `@tsuuanmi/pi-ai` so
reasoning maps, context limits, output limits, compatibility flags, and costs stay synchronized with
Pi's Anthropic transport.
