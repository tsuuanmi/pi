# providers/google/models

Mirrors `src/providers/google/models.ts`.

The explicit Gemini 2.5 Flash and Pro mappings include text/image input, bounded reasoning levels,
context/output limits, and per-million-token costs.
