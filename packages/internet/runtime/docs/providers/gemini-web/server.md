# providers/gemini-web/server

Mirrors `src/providers/gemini-web/server.ts`.

Hosts Gemini health/models/Responses and authenticated administration routes, tracks active abort controllers, bridges normalized events to Responses JSON/SSE, and rejects unsupported compact/canary operations.
