# providers/gemini-web/prompt

Mirrors `src/providers/gemini-web/prompt.ts`.

Rejects tools, images/files, structured output, opaque payloads, and reasoning controls before browser acquisition. It compiles normalized history for a new native chat and only the current user text for an existing chat.
