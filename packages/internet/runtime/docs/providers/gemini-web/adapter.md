# providers/gemini-web/adapter

Mirrors `src/providers/gemini-web/adapter.ts`.

Validates and compiles text before browser acquisition, maps the single reasoning option on Flash or Pro to the internal Extended thinking mode, resolves the native chat by Pi session ID, sends only the current user suffix on continuation, emits normalized text/done events, and records the immutable session-to-chat mapping inside serialized turn execution.
