# providers/gemini-web/config

Mirrors `src/providers/gemini-web/config.ts`.

Composes the browser-owned config with provider routing fields and rejects missing paths, unavailable defaults, and capability markers owned by another provider.
