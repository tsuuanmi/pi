# providers/gemini-web/factory

Mirrors `src/providers/gemini-web/factory.ts`.

Transforms strict runtime config and the verified capability marker into Gemini browser/provider config, filters internal Extended thinking from the public Flash/Pro model list, and exposes the concrete server factory registered by the CLI composition root.
