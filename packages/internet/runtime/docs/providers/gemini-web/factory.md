# providers/gemini-web/factory

Mirrors `src/providers/gemini-web/factory.ts`.

Transforms strict runtime config and the verified capability marker into Gemini browser/provider config and exposes the concrete server factory registered by the CLI composition root.
