# providers/gemini-web/lifecycle/doctor

Mirrors `src/providers/gemini-web/lifecycle/doctor.ts`.

Reports strict browser config, private sanitized storage, marker capabilities, explicit browser-only text-mode policy, and a live isolated-browser authentication/capability probe when stored login state exists.
