# providers/chatgpt-web/protocol/types

Mirrors `src/providers/chatgpt-web/protocol/types.ts`.

Extends the normalized core request with ChatGPT-only replay, compaction, hosted-search, structured-output, and opaque-payload parsing state. Gemini and core modules never depend on these fields.
