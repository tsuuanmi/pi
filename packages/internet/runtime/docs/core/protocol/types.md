# core/protocol/types

Mirrors `src/core/protocol/types.ts`.

Defines provider-neutral normalized requests, messages, content parts, tools, adapter events, continuation metadata, and usage. Provider-specific selectors, wire formats, models, and policy do not belong here.
