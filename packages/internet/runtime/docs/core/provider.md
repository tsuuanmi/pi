# core/provider

Mirrors `src/core/provider.ts`.

Defines generic typed turn-adapter contracts and the explicit runtime server-factory registry. Concrete ChatGPT Web and Gemini Web servers are registered only by `cli.ts`; core never imports provider modules.
