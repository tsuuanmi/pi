# browser/gemini-web/capabilities

Mirrors `src/browser/gemini-web/capabilities.ts`.

Defines Gemini browser capability IDs, label normalization/discovery, and strict versioned capability-marker creation and validation. Provider routing consumes these verified browser facts without owning DOM labels.
