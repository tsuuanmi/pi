# browser/gemini-web/session

Mirrors `src/browser/gemini-web/session.ts`.

Defines the live-verified Gemini URL and DOM selector contract, validates native `/app/<chat-id>` URLs, and maps visible versioned Flash, Thinking, and Pro labels to stable internal IDs.
