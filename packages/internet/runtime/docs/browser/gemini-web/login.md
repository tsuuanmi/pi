# browser/gemini-web/login

Mirrors `src/browser/gemini-web/login.ts`.

Runs headed isolated login, stages imported sanitized storage until fresh-browser verification succeeds, discovers account-visible models, and writes private storage/capability markers only after verification. The non-mutating probe is reused by doctor diagnostics.
