# browser/gemini-web/completion

Mirrors `src/browser/gemini-web/completion.ts`.

Reads the final visible Gemini response and stop-control state into a provider-specific DOM snapshot. Streaming and stability policy are owned by `streaming.ts`.
