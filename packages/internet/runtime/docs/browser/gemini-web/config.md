# browser/gemini-web/config

Mirrors `src/browser/gemini-web/config.ts`.

Defines the browser-session configuration shared by Gemini login, doctor, and turn execution. Provider config composes this browser-owned contract rather than reversing the dependency.
