# browser/gemini-web/auth

Mirrors `src/browser/gemini-web/auth.ts`.

Verifies signed-in Gemini state only from the Google `SignOutOptions` account anchor, distinguishes the signed-out control, and reads/writes the private capability marker without logging account labels or storage values.
