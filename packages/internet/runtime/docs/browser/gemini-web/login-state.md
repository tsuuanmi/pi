# browser/gemini-web/login-state

Mirrors `src/browser/gemini-web/login-state.ts`.

Validates bounded Playwright storage state and retains only `google.com`, `accounts.google.com`, and `gemini.google.com` cookies plus `https://gemini.google.com` origin storage.
